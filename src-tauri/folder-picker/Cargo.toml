[package]
name = "glyphary-folder-picker"
version = "0.1.0"
edition = "2021"
build = "build.rs"
links = "glyphary-folder-picker"

[dependencies]
serde = { version = "1", features = ["derive"] }
tauri = "2"

[build-dependencies]
tauri-plugin = { version = "2.5", features = ["build"] }
