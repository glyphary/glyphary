{
  "id": "uppercase_selection_rust",
  "name": "Uppercase Selection (Rust)",
  "runtime": "glyphary-wasm-transform@1",
  "version": "0.1.0",
  "description": "Sample Rust WASM transform plugin that uppercases the selected text.",
  "permissions": ["selection:read", "selection:write"],
  "commands": [
    {
      "id": "uppercase_selection",
      "title": "Uppercase Selection (Rust)",
      "description": "Transform the current selection to uppercase using a Rust-built WASM module.",
      "wasm": {
        "module": "plugin.wasm",
        "input": "selection",
        "output": "replaceSelection",
        "timeoutMs": 200
      }
    }
  ]
}
